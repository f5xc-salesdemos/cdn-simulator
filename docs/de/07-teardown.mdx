---
title: Abbau
description: >-
  Führen Sie terraform destroy aus, um alle Azure-Ressourcen (Ressourcengruppe,
  VM, VNet, PIP, NSG) zu entfernen, den lokalen Terraform-Zustand zu bereinigen
  und DNS- oder Hosts-Datei-Änderungen rückgängig zu machen.
sidebar:
  order: 7
i18n:
  sourceHash: 2abd4bf5d8c8
  translator: machine
---

Wenn die Lab-Sitzung abgeschlossen ist, zerstören Sie alle Azure-Ressourcen, um die Abrechnung zu stoppen.

## Terraform Destroy

Aus dem Verzeichnis, das die Terraform-Konfiguration enthält:

```bash
# Review what will be destroyed
terraform plan -destroy

# Destroy all resources
terraform destroy
```

Terraform wird zur Bestätigung auffordern. Geben Sie `yes` ein, um fortzufahren.

## Bereinigung überprüfen

Bestätigen Sie, dass alle Ressourcen entfernt wurden:

```bash
# Check resource group is gone
az group show --name "$(terraform output -raw resource_group_name)" 2>&1 \
  | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"
```

## Lokalen Zustand bereinigen

Entfernen Sie Terraform-Zustandsdateien und zwischengespeicherte Dateien:

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## DNS-/Hosts-Änderungen rückgängig machen

Wenn Sie während der [Integration](06-integrate/) DNS- oder Hosts-Dateien geändert haben, machen Sie diese Änderungen rückgängig:

```bash
# Remove hosts file entry (Linux/macOS)
sudo sed -i '/<CDN_EDGE_PUBLIC_IP>/d' /etc/hosts
```

Wenn Sie einen DNS-A-Eintrag erstellt haben, löschen Sie diesen über Ihren DNS-Anbieter.
