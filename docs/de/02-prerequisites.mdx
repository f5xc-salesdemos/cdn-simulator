---
title: Voraussetzungen
description: >-
  Required tools (Azure CLI, Terraform >= 1.5, SSH key pair), Azure subscription
  permissions, and Azure AD requirements for deploying the performance-optimized
  CDN edge node VM
sidebar:
  order: 2
i18n:
  sourceHash: fafd7c61e178
  translator: machine
---

## Erforderlich

### Azure-Abonnement

Ein aktives Azure-Abonnement mit der Berechtigung, Folgendes zu erstellen:

- Ressourcengruppen
- Virtuelle Netzwerke und Subnetze
- Netzwerksicherheitsgruppen
- Öffentliche IP-Adressen
- Virtuelle Maschinen

### Azure CLI

Installieren und authentifizieren Sie die Azure CLI:

```bash
# Install (if not present)
curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash

# Authenticate
az login

# Verify subscription
az account show --query '{name:name, id:id}' -o table
```

### Terraform

Terraform >= 1.5 ist erforderlich:

```bash
# Verify installation
terraform version

# If not installed, use HashiCorp's APT repository
wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update && sudo apt install terraform
```

### SSH-Schlüsselpaar

Ein SSH-Schlüsselpaar für den VM-Zugriff:

```bash
# Generate if you don't have one
ssh-keygen -t ed25519 -f ~/.ssh/cdn-edge-key -N ""
```

### Azure AD-Berechtigungen

Ihr Azure AD-Konto muss über `User.Read`-Berechtigungen verfügen. Der Deployer-Bezeichner (der bei der Ressourcenbenennung verwendet wird) wird automatisch aus Ihrem Azure AD-Profil abgeleitet. Bei der Authentifizierung über einen Dienstprinzipal oder eine verwaltete Identität setzen Sie die Terraform-Variable `deployer` explizit.

## Optional

### Benutzerdefinierte Domain

Wenn Sie echte TLS-Zertifikate anstelle von selbstsignierten verwenden möchten:

- Ein Domainname mit DNS-Kontrolle
- Die Möglichkeit, A-Records zu erstellen, die auf die öffentliche Azure-IP-Adresse verweisen

### Origin-Server-URL

Die URL des F5 XC HTTP Load Balancers, an den dieser Edge-Knoten den Datenverkehr weiterleitet. Dies kann nach dem Deployment konfiguriert werden — die Standard-NGINX-Konfiguration verwendet einen Platzhalter, den Sie in der NGINX-Konfiguration aktualisieren.
