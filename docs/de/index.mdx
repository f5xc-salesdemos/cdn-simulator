---
title: CDN-Simulator
description: >-
  NGINX-basierter CDN-Edge-Node-Simulator, der realistische Header von Akamai,
  Cloudflare, Amazon CloudFront, Fastly und Azure Front Door einfügt. Wird als
  Ubuntu 24.04 Azure-VM via Terraform mit NGINX-Caching-Reverse-Proxy
  bereitgestellt. Wird als modulare Lab-Komponente vor F5 Distributed Cloud
  HTTP-Load-Balancern eingesetzt.
template: splash
sidebar:
  hidden: true
hero:
  tagline: >-
    Stellen Sie einen funktionalen CDN-Edge-Node in Azure für Integrationstests
    mit F5 Distributed Cloud bereit.
  image:
    html: "<div style=\"font-size:4rem;\">\U0001F310</div>"
  actions:
    - text: Erste Schritte
      link: 01-overview/
      icon: right-arrow
      variant: primary
    - text: Jetzt bereitstellen
      link: 03-deploy/
      icon: rocket
      variant: minimal
i18n:
  sourceHash: 7c6769192b97
  translator: machine
---

## Was dieser Simulator bietet

- **[Einzelner Edge-Node](01-overview/)** — Eine Ubuntu 24.04-VM, die NGINX als Caching-Reverse-Proxy ausführt – das minimal funktionsfähige CDN-Edge
- **[Terraform-Bereitstellung](03-deploy/)** — Vollständige Azure-Infrastruktur als Code: VNet, PIP, NSG und VM mit Cloud-init-Provisionierung
- **[67+ CDN-Anbieter-Header](04-nginx-config/)** — Simuliert Akamai, Cloudflare, CloudFront, Fastly und Azure Front Door-Anforderungsheader, einschließlich True-Client-IP, CF-Connecting-IP, Geolokalisierung, Geräteerkennung, JA3/JA4-Fingerprints und Bot-Scores
- **[Ursprungsserver-Integration](06-integrate/)** — Konfigurieren Sie den Edge-Node, um Cache-Misses an einen F5 XC HTTP-Load-Balancer als Ursprungsserver weiterzuleiten
- **[Cache-Verifizierung](05-verify/)** — Validierung von HIT/MISS-Verhalten mit X-Cache-Status-Headern und NGINX-Cache-Inspektion

## Kurzreferenz

Nach der Bereitstellung stellt der CDN-Edge-Node folgende Endpunkte bereit:

| Endpunkt | Methode | Zweck |
|---|---|---|
| `http://<PUBLIC_IP>/health` | GET | Gesundheitsprüfung — gibt JSON mit Komponentenstatus und Anbieterprofilen zurück |
| `http://<PUBLIC_IP>/` | GET | CDN-Proxy — speichert Anfragen im Cache und leitet sie an den Ursprungsserver weiter |
| `http://<PUBLIC_IP>/<any-path>` | GET | CDN-Proxy — alle Pfade werden mit Caching und Anbieter-Headern weitergeleitet |

Antwortheader, die jeder proxierten Antwort hinzugefügt werden:

| Header | Beispiel | Zweck |
|---|---|---|
| `X-Cache-Status` | `HIT` oder `MISS` | Gibt an, ob die Antwort aus dem Cache geliefert wurde |
| `X-CDN-Edge` | `cdn-simulator` | Identifiziert diesen Edge-Node |
| `X-CDN-POP` | `SJC` | Simulierter Point-of-Presence-Code |
| `X-Served-By` | `cache-sjc3120-SJC` | Simulierter Cache-Node-Bezeichner |
| `X-Request-ID` | UUID | Eindeutiger Bezeichner pro Anfrage |

## Dokumentationsübersicht

| Seite | Zielgruppe | Zweck |
|---|---|---|
| [Übersicht](01-overview/) | Mensch + KI | Architektur, Multi-Anbieter-Kontext, was dieser Simulator abbildet |
| [Voraussetzungen](02-prerequisites/) | KI | Erforderliche Werkzeuge, Azure-Abonnement, SSH-Schlüssel, Kostenschätzung |
| [Bereitstellen](03-deploy/) | KI | Vollständiges Terraform-HCL — kopieren und anwenden zur Bereitstellung |
| [NGINX-Konfiguration](04-nginx-config/) | Mensch + KI | Alle 67+ CDN-Anbieter-Header mit genauen Werten und Zwecken |
| [Verifizieren](05-verify/) | KI | Smoke-Test-Befehle mit erwartetem Anfrage-/Antwortverhalten |
| [Integration mit F5 XC](06-integrate/) | Mensch + KI | Edge-Node mit F5 XC als Ursprungsserver verbinden, End-to-End-Verifizierung |
| [Abbau](07-teardown/) | KI | Alle Ressourcen zerstören, Bereinigungsbefehle |
