---
title: सत्यापन
description: >-
  अपेक्षित HTTP प्रतिक्रिया कोड, X-Cache-Status हेडर मान (HIT/MISS), स्वास्थ्य
  जांच JSON प्रतिक्रिया, कैश डायरेक्टरी निरीक्षण, ऑरिजिन कनेक्टिविटी जांच, और
  cloud-init, NGINX स्टार्टअप, और NSG समस्याओं के लिए समस्या निवारण के साथ स्मोक
  टेस्ट कमांड
sidebar:
  order: 5
i18n:
  sourceHash: 939ce6f55688
  translator: machine
---

डिप्लॉयमेंट के बाद, CDN एज नोड सही ढंग से कार्य कर रहा है यह पुष्टि करने के लिए ये जांचें चलाएं। `terraform apply` के बाद cloud-init द्वारा NGINX इंस्टॉलेशन पूरा होने के लिए 2-3 मिनट का समय दें।

## स्वास्थ्य जांच

सत्यापित करें कि NGINX चल रहा है और प्रतिक्रिया दे रहा है:

```bash
curl -s "http://<PUBLIC_IP>/health" | jq .
```

अपेक्षित आउटपुट:

```json
{
  "status": "healthy",
  "component": "cdn-edge",
  "engine": "nginx",
  "vendor_profiles": [
    "akamai",
    "cloudflare",
    "cloudfront",
    "fastly",
    "azure-front-door"
  ]
}
```

यदि Terraform आउटपुट का उपयोग कर रहे हैं:

```bash
curl -s "$(terraform output -raw health_check_url)" | jq .
```

## कैश मिस (पहला अनुरोध)

किसी भी पथ पर पहला अनुरोध कैश मिस होगा — एज ऑरिजिन से डेटा लाता है:

```bash
curl -I "http://<PUBLIC_IP>/"
```

प्रतिक्रिया में इन हेडर्स को देखें:

```
X-Cache-Status: MISS
X-CDN-Edge: cdn-simulator
```

`MISS` का अर्थ है कि सामग्री कैश में नहीं थी और ऑरिजिन सर्वर से लाई गई थी।

## कैश हिट (बाद का अनुरोध)

वही अनुरोध तुरंत दोहराएं:

```bash
curl -I "http://<PUBLIC_IP>/"
```

अपेक्षित हेडर्स:

```
X-Cache-Status: HIT
X-CDN-Edge: cdn-simulator
```

`HIT` पुष्टि करता है कि प्रतिक्रिया ऑरिजिन से संपर्क किए बिना NGINX डिस्क कैश से दी गई थी।

## कैश डायरेक्टरी निरीक्षण

डिस्क पर कैश्ड सामग्री मौजूद है यह सत्यापित करने के लिए VM में SSH करें:

```bash
ssh azureuser@<PUBLIC_IP>

# Check cache directory has content
sudo find /var/cache/nginx/cdn -type f | head -20

# Check cache size
sudo du -sh /var/cache/nginx/cdn
```

## NGINX स्थिति

सत्यापित करें कि NGINX चल रहा है और कॉन्फ़िगरेशन मान्य है:

```bash
ssh azureuser@<PUBLIC_IP>

# Check service status
sudo systemctl status nginx

# Validate configuration
sudo nginx -t

# View active connections (if stub_status is enabled)
curl -s http://localhost/health
```

## ऑरिजिन कनेक्टिविटी

सत्यापित करें कि एज नोड ऑरिजिन सर्वर तक पहुंच सकता है:

```bash
ssh azureuser@<PUBLIC_IP>

# Test connectivity to origin (replace with your origin URL)
curl -I "https://your-origin.example.com/"
```

यदि यह विफल होता है, तो जांचें:

1. Terraform कॉन्फ़िग में `origin_server` वेरिएबल सही है
2. ऑरिजिन सर्वर एज नोड के पब्लिक IP से इनबाउंड कनेक्शन की अनुमति देता है
3. VM से DNS रिज़ॉल्यूशन काम करता है (`nslookup your-origin.example.com`)

## Cloud-Init प्रगति

cloud-init लॉग के माध्यम से प्रोविज़निंग प्रगति की निगरानी करें:

```bash
ssh azureuser@<PUBLIC_IP> "tail -f /var/log/cloud-init-progress.log"
```

अपेक्षित चरण: `[init]`, `[nic]` (डायनामिक NIC डिटेक्शन), `[complete]`।

यदि cloud-init त्रुटियां रिपोर्ट करता है:

```bash
ssh azureuser@<PUBLIC_IP> "sudo cloud-init status --long"
```

## एंड-टू-एंड परीक्षण

एक पूर्ण अनुरोध चक्र चलाएं और प्रतिक्रिया श्रृंखला सत्यापित करें:

```bash
# First request — MISS (fetches from origin)
echo "=== Request 1 (expect MISS) ==="
curl -s -o /dev/null -w "HTTP %{http_code} | Cache: %{header:X-Cache-Status}\n" "http://<PUBLIC_IP>/test-path"

# Second request — HIT (served from cache)
echo "=== Request 2 (expect HIT) ==="
curl -s -o /dev/null -w "HTTP %{http_code} | Cache: %{header:X-Cache-Status}\n" "http://<PUBLIC_IP>/test-path"
```

अपेक्षित आउटपुट:

```
=== Request 1 (expect MISS) ===
HTTP 200 | Cache: MISS
=== Request 2 (expect HIT) ===
HTTP 200 | Cache: HIT
```

## वेंडर हेडर सत्यापन

सभी CDN वेंडर हेडर्स इंजेक्ट हो रहे हैं यह सत्यापित करने के लिए `/headers` पथ पर अनुरोध करें (जब httpbin.org को ऑरिजिन के रूप में उपयोग कर रहे हों):

```bash
curl -s "http://<PUBLIC_IP>/headers" | python3 -m json.tool
```

अपेक्षित प्रतिक्रिया में सभी पांच वेंडर्स के हेडर्स शामिल हैं:

```json
{
    "headers": {
        "True-Client-Ip": "<YOUR_CLIENT_IP>",
        "Cf-Connecting-Ip": "<YOUR_CLIENT_IP>",
        "Cf-Ipcountry": "US",
        "Cf-Ray": "<request_id>-SJC",
        "Cf-Bot-Score": "85",
        "Cf-Ja3-Hash": "e7d705a3286e19ea42f587b344ee6865",
        "Cloudfront-Viewer-Country": "US",
        "Cloudfront-Viewer-City": "San Jose",
        "Cloudfront-Is-Desktop-Viewer": "true",
        "Cloudfront-Is-Mobile-Viewer": "false",
        "Fastly-Client-Ip": "<YOUR_CLIENT_IP>",
        "X-Akamai-Edgescape": "georegion=263,country_code=US,...",
        "X-Azure-Clientip": "<YOUR_CLIENT_IP>",
        "X-Azure-Fdid": "a0a0a0a0-bbbb-cccc-dddd-e1e1e1e1e1e1",
        "X-Geo-Country-Code": "US"
    }
}
```

### डिवाइस डिटेक्शन परीक्षण

डिवाइस डिटेक्शन हेडर्स बदलते हैं यह सत्यापित करने के लिए मोबाइल User-Agent के साथ परीक्षण करें:

```bash
curl -s "http://<PUBLIC_IP>/headers?device=mobile" \
  -H "User-Agent: Mozilla/5.0 (iPhone; CPU iPhone OS 17_0 like Mac OS X)" \
  | python3 -m json.tool | grep -E "Is-Mobile|Is-Desktop|is_mobile"
```

अपेक्षित:

```
"Cloudfront-Is-Desktop-Viewer": "false",
"Cloudfront-Is-Mobile-Viewer": "true",
```

## समस्या निवारण

### NGINX नहीं चल रहा

```bash
ssh azureuser@<PUBLIC_IP>
sudo systemctl status nginx
sudo journalctl -u nginx --no-pager -n 50
```

### Cloud-init पूरा नहीं हुआ

`terraform apply` के बाद, cloud-init को 2-3 मिनट लगते हैं। प्रगति जांचें:

```bash
ssh azureuser@<PUBLIC_IP>
sudo cloud-init status
sudo tail -f /var/log/cloud-init-output.log
```

### कैश हमेशा MISS दिखा रहा है

- सत्यापित करें कि ऑरिजिन कैश करने योग्य प्रतिक्रिया कोड लौटाता है (200, 301, 302)
- जांचें कि ऑरिजिन `Cache-Control: no-cache` या `no-store` हेडर्स नहीं भेजता
- NGINX एरर लॉग्स जांचें: `sudo tail -f /var/log/nginx/error.log`

### पोर्ट 80/443 पर कनेक्शन अस्वीकृत

- सत्यापित करें कि NSG नियम लागू हैं: `az network nsg rule list --nsg-name "$(terraform output -raw nsg_name)" --resource-group "$(terraform output -raw resource_group_name)" -o table`
- सत्यापित करें कि NGINX सुन रहा है: `ssh azureuser@<PUBLIC_IP> "sudo ss -tlnp | grep nginx"`
