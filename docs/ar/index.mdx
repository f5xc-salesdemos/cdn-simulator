---
title: محاكي CDN
description: >-
  محاكي عقدة حافة CDN مبني على NGINX يضخ رؤوسًا واقعية من Akamai وCloudflare
  وAmazon CloudFront وFastly وAzure Front Door. يُنشر كجهاز افتراضي Azure يعمل
  بنظام Ubuntu 24.04 عبر Terraform مع وكيل عكسي مع التخزين المؤقت NGINX. يُستخدم
  كمكوّن معملي معياري أمام موازنات تحميل HTTP الخاصة بـ F5 Distributed Cloud.
template: splash
sidebar:
  hidden: true
hero:
  tagline: انشر عقدة حافة CDN وظيفية في Azure لاختبار التكامل مع F5 Distributed Cloud.
  image:
    html: "<div style=\"font-size:4rem;\">\U0001F310</div>"
  actions:
    - text: ابدأ الآن
      link: 01-overview/
      icon: right-arrow
      variant: primary
    - text: انشر الآن
      link: 03-deploy/
      icon: rocket
      variant: minimal
i18n:
  sourceHash: 7c6769192b97
  translator: machine
---

## ما الذي يوفره هذا

- **[عقدة حافة واحدة](01-overview/)** — جهاز افتراضي واحد يعمل بنظام Ubuntu 24.04 يشغّل NGINX كوكيل عكسي مع التخزين المؤقت، وهو الحد الأدنى القابل للتطبيق لعقدة حافة CDN
- **[نشر Terraform](03-deploy/)** — بنية تحتية كاملة لـ Azure كرمز: VNet وPIP وNSG وجهاز افتراضي مع توفير cloud-init
- **[أكثر من 67 رأسًا لموردي CDN](04-nginx-config/)** — يحاكي رؤوس طلبات Akamai وCloudflare وCloudFront وFastly وAzure Front Door بما فيها True-Client-IP وCF-Connecting-IP والموقع الجغرافي وكشف الجهاز وبصمات JA3/JA4 ودرجات البوت
- **[تكامل خادم المصدر](06-integrate/)** — ضبط الحافة لإعادة توجيه الإخفاقات في التخزين المؤقت إلى موازن تحميل HTTP الخاص بـ F5 XC كمصدر
- **[التحقق من التخزين المؤقت](05-verify/)** — التحقق من سلوك HIT/MISS مع رؤوس X-Cache-Status وفحص التخزين المؤقت في NGINX

## المرجع السريع

بعد النشر، تعرض عقدة حافة CDN:

| نقطة النهاية | الطريقة | الغرض |
|---|---|---|
| `http://<PUBLIC_IP>/health` | GET | فحص الحالة — يُعيد JSON مع حالة المكوّنات وملفات تعريف الموردين |
| `http://<PUBLIC_IP>/` | GET | وكيل CDN — يخزّن الطلبات مؤقتًا ويعيد توجيهها إلى الخادم المصدر |
| `http://<PUBLIC_IP>/<any-path>` | GET | وكيل CDN — جميع المسارات تُوكَّل مع التخزين المؤقت ورؤوس الموردين |

رؤوس الاستجابة المُضافة إلى كل استجابة موكَّلة:

| الرأس | مثال | الغرض |
|---|---|---|
| `X-Cache-Status` | `HIT` أو `MISS` | ما إذا كانت الاستجابة قد جرى تقديمها من التخزين المؤقت |
| `X-CDN-Edge` | `cdn-simulator` | يعرّف عقدة الحافة هذه |
| `X-CDN-POP` | `SJC` | رمز نقطة الحضور المحاكاة |
| `X-Served-By` | `cache-sjc3120-SJC` | معرّف عقدة التخزين المؤقت المحاكاة |
| `X-Request-ID` | UUID | معرّف فريد لكل طلب |

## دليل التوثيق

| الصفحة | الجمهور | الغرض |
|---|---|---|
| [نظرة عامة](01-overview/) | الإنسان + الذكاء الاصطناعي | البنية المعمارية والسياق متعدد الموردين وما تحاكيه |
| [المتطلبات الأساسية](02-prerequisites/) | الذكاء الاصطناعي | الأدوات المطلوبة واشتراك Azure ومفاتيح SSH وتقدير التكلفة |
| [النشر](03-deploy/) | الذكاء الاصطناعي | HCL الكامل لـ Terraform — انسخ وطبّق للنشر |
| [تهيئة NGINX](04-nginx-config/) | الإنسان + الذكاء الاصطناعي | جميع رؤوس موردي CDN البالغة 67+ مع القيم الدقيقة والأغراض |
| [التحقق](05-verify/) | الذكاء الاصطناعي | أوامر اختبار الدخان مع سلوك الطلب/الاستجابة المتوقع |
| [التكامل مع F5 XC](06-integrate/) | الإنسان + الذكاء الاصطناعي | توصيل الحافة بـ F5 XC كمصدر، والتحقق الشامل من البداية إلى النهاية |
| [إزالة التثبيت](07-teardown/) | الذكاء الاصطناعي | تدمير جميع الموارد وأوامر التنظيف |
