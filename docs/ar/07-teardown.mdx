---
title: التفكيك
description: >-
  شغّل أمر terraform destroy لإزالة جميع موارد Azure (مجموعة الموارد، الجهاز
  الافتراضي، الشبكة الافتراضية، عنوان IP العام، مجموعة أمان الشبكة)، وتنظيف حالة
  Terraform المحلية، والتراجع عن تغييرات DNS أو ملف hosts.
sidebar:
  order: 7
i18n:
  sourceHash: 2abd4bf5d8c8
  translator: machine
---

عند اكتمال جلسة المختبر، دمِّر جميع موارد Azure لإيقاف الفوترة.

## التدمير باستخدام Terraform

من الدليل الذي يحتوي على تكوين Terraform:

```bash
# Review what will be destroyed
terraform plan -destroy

# Destroy all resources
terraform destroy
```

سيطلب Terraform تأكيداً. اكتب `yes` للمتابعة.

## التحقق من التنظيف

تأكد من إزالة جميع الموارد:

```bash
# Check resource group is gone
az group show --name "$(terraform output -raw resource_group_name)" 2>&1 \
  | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"
```

## تنظيف الحالة المحلية

أزل حالة Terraform والملفات المؤقتة المخزّنة مؤقتاً:

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## التراجع عن تغييرات DNS/Hosts

إذا قمت بتعديل DNS أو ملفات hosts أثناء [التكامل](06-integrate/)، تراجع عن تلك التغييرات:

```bash
# Remove hosts file entry (Linux/macOS)
sudo sed -i '/<CDN_EDGE_PUBLIC_IP>/d' /etc/hosts
```

إذا أنشأت سجل DNS من النوع A، فاحذفه من خلال مزود DNS الخاص بك.
