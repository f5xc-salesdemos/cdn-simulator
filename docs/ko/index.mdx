---
title: CDN 시뮬레이터
description: >-
  Akamai, Cloudflare, Amazon CloudFront, Fastly, Azure Front Door의 실제 헤더를 삽입하는
  NGINX 기반 CDN 엣지 노드 시뮬레이터입니다. Terraform을 통해 NGINX 캐싱 역방향 프록시가 포함된 Ubuntu 24.04
  Azure VM으로 배포됩니다. F5 Distributed Cloud HTTP 부하 분산기 앞에서 모듈식 랩 구성 요소로 사용됩니다.
template: splash
sidebar:
  hidden: true
hero:
  tagline: F5 Distributed Cloud와의 통합 테스트를 위해 Azure에 기능적인 CDN 엣지 노드를 배포하세요.
  image:
    html: "<div style=\"font-size:4rem;\">\U0001F310</div>"
  actions:
    - text: 시작하기
      link: 01-overview/
      icon: right-arrow
      variant: primary
    - text: 지금 배포
      link: 03-deploy/
      icon: rocket
      variant: minimal
i18n:
  sourceHash: 7c6769192b97
  translator: machine
---

## 제공 기능

- **[단일 엣지 노드](01-overview/)** — 캐싱 역방향 프록시로 NGINX를 실행하는 Ubuntu 24.04 VM 1대, 최소 실행 가능한 CDN 엣지
- **[Terraform 배포](03-deploy/)** — 완전한 Azure 인프라 코드: VNet, PIP, NSG, cloud-init 프로비저닝이 포함된 VM
- **[67개 이상의 CDN 벤더 헤더](04-nginx-config/)** — True-Client-IP, CF-Connecting-IP, 지리적 위치, 디바이스 감지, JA3/JA4 핑거프린트, 봇 점수를 포함한 Akamai, Cloudflare, CloudFront, Fastly, Azure Front Door 요청 헤더 시뮬레이션
- **[오리진 통합](06-integrate/)** — 캐시 미스를 오리진 서버로서 F5 XC HTTP 부하 분산기에 전달하도록 엣지 구성
- **[캐시 검증](05-verify/)** — X-Cache-Status 헤더와 NGINX 캐시 검사를 통한 HIT/MISS 동작 확인

## 빠른 참조

배포 후, CDN 엣지 노드는 다음을 노출합니다:

| 엔드포인트 | 메서드 | 목적 |
|---|---|---|
| `http://<PUBLIC_IP>/health` | GET | 상태 확인 — 구성 요소 상태 및 벤더 프로필이 포함된 JSON 반환 |
| `http://<PUBLIC_IP>/` | GET | CDN 프록시 — 오리진 서버로의 요청을 캐시하고 전달 |
| `http://<PUBLIC_IP>/<any-path>` | GET | CDN 프록시 — 모든 경로는 캐싱 및 벤더 헤더와 함께 프록시 처리 |

모든 프록시 응답에 추가되는 응답 헤더:

| 헤더 | 예시 | 목적 |
|---|---|---|
| `X-Cache-Status` | `HIT` 또는 `MISS` | 응답이 캐시에서 제공되었는지 여부 |
| `X-CDN-Edge` | `cdn-simulator` | 이 엣지 노드를 식별 |
| `X-CDN-POP` | `SJC` | 시뮬레이션된 거점(Point of Presence) 코드 |
| `X-Served-By` | `cache-sjc3120-SJC` | 시뮬레이션된 캐시 노드 식별자 |
| `X-Request-ID` | UUID | 요청별 고유 식별자 |

## 문서 가이드

| 페이지 | 대상 | 목적 |
|---|---|---|
| [개요](01-overview/) | 사람 + AI | 아키텍처, 멀티 벤더 컨텍스트, 시뮬레이션 대상 |
| [사전 요구 사항](02-prerequisites/) | AI | 필요한 도구, Azure 구독, SSH 키, 비용 추정 |
| [배포](03-deploy/) | AI | 완전한 Terraform HCL — 복사 후 적용하여 배포 |
| [NGINX 구성](04-nginx-config/) | 사람 + AI | 정확한 값과 목적이 포함된 67개 이상의 CDN 벤더 헤더 |
| [검증](05-verify/) | AI | 예상 요청/응답 동작이 포함된 스모크 테스트 명령 |
| [F5 XC와 통합](06-integrate/) | 사람 + AI | 오리진 서버로 F5 XC에 엣지 연결, 종단 간 검증 |
| [해체](07-teardown/) | AI | 모든 리소스 삭제, 정리 명령 |
