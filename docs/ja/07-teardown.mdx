---
title: ティアダウン
description: >-
  terraform destroy を実行して Azure リソース（リソースグループ、VM、VNet、PIP、NSG）をすべて削除し、ローカルの
  Terraform ステートをクリーンアップして、DNS またはホストファイルの変更を元に戻します。
sidebar:
  order: 7
i18n:
  sourceHash: 2abd4bf5d8c8
  translator: machine
---

ラボセッションが完了したら、課金を停止するためにすべての Azure リソースを破棄してください。

## Terraform Destroy

Terraform 設定が含まれるディレクトリから実行します：

```bash
# Review what will be destroyed
terraform plan -destroy

# Destroy all resources
terraform destroy
```

Terraform は確認を求めるプロンプトを表示します。続行するには `yes` と入力してください。

## クリーンアップの確認

すべてのリソースが削除されたことを確認します：

```bash
# Check resource group is gone
az group show --name "$(terraform output -raw resource_group_name)" 2>&1 \
  | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"
```

## ローカルステートのクリーンアップ

Terraform ステートおよびキャッシュファイルを削除します：

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## DNS/ホストファイルの変更を元に戻す

[インテグレーション](06-integrate/) 中に DNS またはホストファイルを変更した場合は、その変更を元に戻してください：

```bash
# Remove hosts file entry (Linux/macOS)
sudo sed -i '/<CDN_EDGE_PUBLIC_IP>/d' /etc/hosts
```

DNS の A レコードを作成した場合は、DNS プロバイダーのインターフェースから削除してください。
