---
title: Démontage
description: >-
  Exécutez terraform destroy pour supprimer toutes les ressources Azure (groupe
  de ressources, VM, VNet, PIP, NSG), nettoyez l'état Terraform local et annulez
  les modifications DNS ou du fichier hosts.
sidebar:
  order: 7
i18n:
  sourceHash: 2abd4bf5d8c8
  translator: machine
---

Lorsque la session de laboratoire est terminée, détruisez toutes les ressources Azure pour arrêter la facturation.

## Destruction Terraform

Depuis le répertoire contenant la configuration Terraform :

```bash
# Review what will be destroyed
terraform plan -destroy

# Destroy all resources
terraform destroy
```

Terraform demandera une confirmation. Tapez `yes` pour continuer.

## Vérifier le nettoyage

Confirmez que toutes les ressources ont été supprimées :

```bash
# Check resource group is gone
az group show --name "$(terraform output -raw resource_group_name)" 2>&1 \
  | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"
```

## Nettoyer l'état local

Supprimez l'état Terraform et les fichiers en cache :

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## Annuler les modifications DNS/Hosts

Si vous avez modifié le DNS ou les fichiers hosts pendant l'[intégration](06-integrate/), annulez ces modifications :

```bash
# Remove hosts file entry (Linux/macOS)
sudo sed -i '/<CDN_EDGE_PUBLIC_IP>/d' /etc/hosts
```

Si vous avez créé un enregistrement DNS de type A, supprimez-le via votre fournisseur DNS.
