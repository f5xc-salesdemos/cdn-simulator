---
title: Smantellamento
description: >-
  Eseguire terraform destroy per rimuovere tutte le risorse Azure (gruppo di
  risorse, VM, VNet, PIP, NSG), pulire lo stato locale di Terraform e
  ripristinare le modifiche al DNS o al file hosts.
sidebar:
  order: 7
i18n:
  sourceHash: 2abd4bf5d8c8
  translator: machine
---

Quando la sessione del laboratorio è completata, distruggere tutte le risorse Azure per interrompere la fatturazione.

## Terraform Destroy

Dalla directory contenente la configurazione Terraform:

```bash
# Review what will be destroyed
terraform plan -destroy

# Destroy all resources
terraform destroy
```

Terraform chiederà conferma. Digitare `yes` per procedere.

## Verificare la pulizia

Confermare che tutte le risorse siano state rimosse:

```bash
# Check resource group is gone
az group show --name "$(terraform output -raw resource_group_name)" 2>&1 \
  | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"
```

## Pulire lo stato locale

Rimuovere lo stato di Terraform e i file memorizzati nella cache:

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## Ripristinare le modifiche DNS/Hosts

Se sono stati modificati il DNS o i file hosts durante l'[integrazione](06-integrate/), ripristinare tali modifiche:

```bash
# Remove hosts file entry (Linux/macOS)
sudo sed -i '/<CDN_EDGE_PUBLIC_IP>/d' /etc/hosts
```

Se è stato creato un record DNS A, eliminarlo tramite il proprio provider DNS.
