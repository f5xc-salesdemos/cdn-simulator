---
title: Prerequisiti
description: >-
  Strumenti richiesti (Azure CLI, Terraform >= 1.5, coppia di chiavi SSH),
  permessi della sottoscrizione Azure e requisiti di Azure AD per il deployment
  della VM del nodo edge CDN ottimizzata per le prestazioni
sidebar:
  order: 2
i18n:
  sourceHash: fafd7c61e178
  translator: machine
---

## Requisiti

### Sottoscrizione Azure

Una sottoscrizione Azure attiva con i permessi per creare:

- Gruppi di risorse
- Reti virtuali e subnet
- Gruppi di sicurezza di rete
- Indirizzi IP pubblici
- Macchine virtuali

### Azure CLI

Installare e autenticarsi con Azure CLI:

```bash
# Install (if not present)
curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash

# Authenticate
az login

# Verify subscription
az account show --query '{name:name, id:id}' -o table
```

### Terraform

È richiesto Terraform >= 1.5:

```bash
# Verify installation
terraform version

# If not installed, use HashiCorp's APT repository
wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update && sudo apt install terraform
```

### Coppia di chiavi SSH

Una coppia di chiavi SSH per l'accesso alla VM:

```bash
# Generate if you don't have one
ssh-keygen -t ed25519 -f ~/.ssh/cdn-edge-key -N ""
```

### Permessi Azure AD

Il vostro account Azure AD deve avere i permessi `User.Read`. L'identificatore del deployer (utilizzato nella denominazione delle risorse) viene derivato automaticamente dal vostro profilo Azure AD. Per l'autenticazione tramite service principal o managed identity, impostare esplicitamente la variabile Terraform `deployer`.

## Opzionali

### Dominio personalizzato

Se desiderate certificati TLS reali invece di certificati auto-firmati:

- Un nome di dominio con controllo DNS
- La possibilità di creare record A che puntano all'IP pubblico Azure

### URL del server di origine

L'URL del load balancer HTTP F5 XC a cui questo nodo edge inoltrerà il traffico. Questo può essere configurato dopo il deployment — la configurazione NGINX predefinita utilizza un segnaposto che si aggiorna nella configurazione di NGINX.
